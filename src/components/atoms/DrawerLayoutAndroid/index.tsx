import {styled} from 'nativewind';
import {DrawerLayoutAndroid as RNDrawerLayoutAndroid} from 'react-native';

const DrawerLayoutAndroid = styled(RNDrawerLayoutAndroid, {});

export default DrawerLayoutAndroid;
